target C {
    threading: false,
    build: "../scripts/build_flexpret_unix.sh"
};

preamble {=
    #include <flexpret_io.h>
=}

main reactor {
    reaction(startup) {=
        _fp_print(0x00000001);
    =}
}
